# observe-ai is a pure static SPA — no runtime environment variables required.
# All data (GeoJSON, Parquet) is bundled into the build from public/data/.
#
# If you add runtime config later (e.g. VITE_ tokens), document them here.
