# Phase 3 pipeline dependencies. Kept minimal on purpose — anything that isn't
# used by a fetcher/normalizer/exporter should not live here.
requests>=2.32,<3
pandas>=2.2,<3
geopandas>=1.0,<2
geopy>=2.4,<3
thefuzz>=0.22,<1
pyarrow>=17,<22
shapely>=2.0,<3
tenacity>=9.0,<10
pyyaml>=6.0,<7
